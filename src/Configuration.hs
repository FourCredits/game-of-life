module Configuration where

import Graphics.Gloss.Data.Color

background :: Color
background = black

cellColor = green
cellColor :: Color

gridSize :: (Int, Int)
gridSize = (40, 40)

windowSize :: (Int, Int)
windowSize = (400, 400)
