module Configuration where

import Graphics.Gloss.Data.Color

background :: Color
background = black

cellColor = green
cellColor :: Color

gridSize :: (Int, Int)
gridSize = (10, 10)

windowSize :: (Int, Int)
windowSize = (400, 400)
